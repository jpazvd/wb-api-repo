# doc/architecture/

Architecture documentation (Phase 8).

Planned: `ARCHITECTURE.md` + module diagram covering the Stata command layer,
YAML metadata pipeline, HTTP cache, and Python CLI.

Empty placeholder until Phase 8 lands.
