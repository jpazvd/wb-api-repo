# src/y/

Embedded `yaml` Stata frame library (Phase 2).

Planned files (mirroring `wbopendata-dev`):

- `yaml.ado` + 12 subcommands (`yaml_read.ado`, `yaml_get.ado`,
  `yaml_write.ado`, `yaml_list.ado`, `yaml_describe.ado`, `yaml_dir.ado`,
  `yaml_clear.ado`, `yaml_validate.ado`, `yaml_frames.ado`, …)
- `yaml.sthlp`, `yaml_examples.sthlp`, `yaml_whatsnew.sthlp`

YAML metadata files (`_wbopendata_indicators.yaml`, `_wbopendata_sources.yaml`,
`_wbopendata_topics.yaml`) live in `src/_/` alongside the `.ado` helpers,
not here — that's the convention `wbopendata-dev` enforces in
`src/wbopendata.pkg`. Generated by `src/py/update_metadata.py` (Phase 1).

Empty placeholder until Phase 2 lands.
