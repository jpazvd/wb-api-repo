# src/y/

YAML metadata store (Phase 1+) and the embedded `yaml` Stata frame library (Phase 2).

Planned files (mirroring `wbopendata-dev`):
- `_wbopendata_parameters.yaml`, `_wbopendata_indicators.yaml`, `_wbopendata_sources.yaml`, `_wbopendata_topics.yaml`
- `yaml.ado` + 12 subcommands (`yaml_read.ado`, `yaml_get.ado`, etc.)

Empty placeholder until Phases 1–2 land.
